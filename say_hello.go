package go_say_hello

func SayHello(nama string) string {
	return "Halooo" + nama + "!"
}

// membuat tags di github dengan command : git tag v0.0.*
// kemudian push dengan command : git push origin v0.0.*